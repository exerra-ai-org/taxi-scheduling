# ── API ─────────────────────────────────────────────────────────────────────────
# Backend base URL — no trailing slash.
# Dev default (when unset): http://localhost:3000
# Prod: set to your backend URL in .env.production
# VITE_API_BASE_URL=https://api.londonlutontaxi.com
